/** Точка входа отдельного бандла: страница заметки грузит его только когда в ней
 *  есть схема. В общий SPA-бандл mermaid не тащим — он весит больше всего сайта. */
import mermaid from 'mermaid';

export default mermaid;
